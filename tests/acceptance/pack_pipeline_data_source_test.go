// Code generated by tools/codegen. DO NOT EDIT.

package tests

import (
	"testing"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
)

func TestPackPipelineDataSources(t *testing.T) {

	resource.Test(t, resource.TestCase{
		ProtoV6ProviderFactories:  providerFactory,
		PreventPostDestroyRefresh: true,
		Steps: []resource.TestStep{
			{
				Config: `resource "criblio_pack_pipeline" "my_packpipeline" {
  group_id = "default"
  id       = "my_id"
  pack     = criblio_pack.pipeline_pack.id
  conf = {
    async_func_timeout = 9066
    description        = "my_description"
    functions = [
      {
        id     = "serde"
        filter = "true"
        conf = jsonencode({
          mode      = "extract"
          type      = "json"
          src_field = "_raw"
        })
      },
      {
        id     = "eval"
        filter = "true"
        conf = jsonencode({
          add = [
            {
              disabled = false
              name     = "_value"
              value    = "1"
            }
          ]
          remove = [
            "host",
            "_raw",
            "source",
            "cribl_breaker",
          ]
        })
      },
      {
        id     = "publish_metrics"
        filter = "true"
        conf = jsonencode({
          overwrite = false
          dimensions = [
            "!_*",
            "*",
          ],
          removeDimensions = ["cribl_pipe"]
          fields = [
            {
              metricType   = "gauge"
              inFieldName  = "_value"
              outFieldExpr = "saas_env"
            }
          ]
        })
      },
    ]
    groups = {
      default = {
        name = "default"
      }
    }
    streamtags = [
      "tags"
    ]
  }
}

resource "criblio_pack" "pipeline_pack" {
  id           = "pack-with-pipeline"
  group_id     = "default"
  description  = "Pack with pipeline"
  disabled     = true
  display_name = "Pack with pipeline"
  source       = "file:/opt/cribl_data/failover/groups/default/default/HelloPacks"
  version      = "1.0.0"
}

data "criblio_pack_pipeline" "by_id" {
  group_id = criblio_pack_pipeline.my_packpipeline.group_id
  id = criblio_pack_pipeline.my_packpipeline.id
  pack = criblio_pack_pipeline.my_packpipeline.pack
  depends_on = [criblio_pack_pipeline.my_packpipeline]
}`,
				Check: resource.ComposeAggregateTestCheckFunc(
					resource.TestCheckResourceAttrPair("data.criblio_pack_pipeline.by_id", "group_id", "criblio_pack_pipeline.my_packpipeline", "group_id"),
					resource.TestCheckResourceAttrPair("data.criblio_pack_pipeline.by_id", "id", "criblio_pack_pipeline.my_packpipeline", "id"),
					resource.TestCheckResourceAttrPair("data.criblio_pack_pipeline.by_id", "pack", "criblio_pack_pipeline.my_packpipeline", "pack"),
				),
			},
		},
	})
}
