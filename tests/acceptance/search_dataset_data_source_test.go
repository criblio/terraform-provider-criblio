// Code generated by tools/codegen. DO NOT EDIT.

package tests

import (
	"os"
	"testing"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
)

func TestSearchDatasetDataSources(t *testing.T) {
	if os.Getenv("DEPLOYMENT") == "onprem" {
		t.Skip("Skipping data source acceptance test for On-Prem deployments as it is not supported")
	}

	resource.Test(t, resource.TestCase{
		ProtoV6ProviderFactories:  providerFactory,
		PreventPostDestroyRefresh: true,
		Steps: []resource.TestStep{
			{
				Config: `resource "criblio_search_dataset" "my_searchdataset" {
  s3_dataset = {
    auto_detect_region = false
    bucket             = "example-search-logs"
    description        = "S3 search dataset"
    filter             = "true"
    id                 = "example_s3_dataset"
    metadata = {
      enable_acceleration = false
    }
    path        = "logs/*.log"
    provider_id = "S3"
    region      = "us-east-1"
    storage_classes = [
      "STANDARD",
    ]
    type = "s3"
  }
}

data "criblio_search_dataset" "by_id" {
  group_id = "default_search"
  id = criblio_search_dataset.my_searchdataset.id
  depends_on = [criblio_search_dataset.my_searchdataset]
}`,
				Check: resource.ComposeAggregateTestCheckFunc(
					resource.TestCheckResourceAttrPair("data.criblio_search_dataset.by_id", "id", "criblio_search_dataset.my_searchdataset", "id"),
				),
			},
		},
	})
}
