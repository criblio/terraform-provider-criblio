// Code generated by tools/codegen. DO NOT EDIT.

package tests

import (
	"testing"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
)

func TestSecretDataSources(t *testing.T) {

	resource.Test(t, resource.TestCase{
		ProtoV6ProviderFactories:  providerFactory,
		PreventPostDestroyRefresh: true,
		Steps: []resource.TestStep{
			{
				Config: `resource "criblio_secret" "my_secret" {
  description = "Database credentials"
  group_id = "default"
  id = "db-credentials"
  password = "db_password"
  secret_type = "credentials"
  username = "db_user"
}

data "criblio_secret" "by_id" {
  group_id = criblio_secret.my_secret.group_id
  id = criblio_secret.my_secret.id
  depends_on = [criblio_secret.my_secret]
}`,
				Check: resource.ComposeAggregateTestCheckFunc(
					resource.TestCheckResourceAttrPair("data.criblio_secret.by_id", "group_id", "criblio_secret.my_secret", "group_id"),
					resource.TestCheckResourceAttrPair("data.criblio_secret.by_id", "id", "criblio_secret.my_secret", "id"),
				),
			},
		},
	})
}
