// Code generated by tools/codegen. DO NOT EDIT.

package tests

import (
	"os"
	"testing"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
)

func TestSearchSavedQueryDataSources(t *testing.T) {
	if os.Getenv("DEPLOYMENT") == "onprem" {
		t.Skip("Skipping data source acceptance test for On-Prem deployments as it is not supported")
	}

	resource.Test(t, resource.TestCase{
		ProtoV6ProviderFactories:  providerFactory,
		PreventPostDestroyRefresh: true,
		Steps: []resource.TestStep{
			{
				Config: `resource "criblio_search_saved_query" "my_search_saved_query" {
  description = "Saved search for recent Windows security channel errors."
  earliest = "-24h"
  id = "saved_winlog_errors"
  is_private = true
  latest = "now"
  name = "Windows security errors"
  query = "dataset=\"windows_events\" | limit 200"
}

data "criblio_search_saved_query" "by_id" {
  group_id = "default_search"
  id = criblio_search_saved_query.my_search_saved_query.id
  depends_on = [criblio_search_saved_query.my_search_saved_query]
}`,
				Check: resource.ComposeAggregateTestCheckFunc(
					resource.TestCheckResourceAttrPair("data.criblio_search_saved_query.by_id", "id", "criblio_search_saved_query.my_search_saved_query", "id"),
				),
			},
		},
	})
}
