// Code generated by tools/codegen. DO NOT EDIT.

package tests

import (
	"testing"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
)

func TestCriblLakeDatasetDataSources(t *testing.T) {

	resource.Test(t, resource.TestCase{
		ProtoV6ProviderFactories:  providerFactory,
		PreventPostDestroyRefresh: true,
		Steps: []resource.TestStep{
			{
				Config: `resource "criblio_cribl_lake_dataset" "my_cribl_lake_dataset" {
  accelerated_fields = [
    "host",
    "status",
  ]
  bucket_name = "lake-tfprovider-beautiful-nguyen-y8y4azd"
  description = "Web server access logs"
  format = "json"
  id = "web_access_logs"
  lake_id = "default"
  retention_period_in_days = 90
}

data "criblio_cribl_lake_dataset" "by_id" {
  lake_id = criblio_cribl_lake_dataset.my_cribl_lake_dataset.lake_id
  id = criblio_cribl_lake_dataset.my_cribl_lake_dataset.id
  depends_on = [criblio_cribl_lake_dataset.my_cribl_lake_dataset]
}`,
				Check: resource.ComposeAggregateTestCheckFunc(
					resource.TestCheckResourceAttrPair("data.criblio_cribl_lake_dataset.by_id", "lake_id", "criblio_cribl_lake_dataset.my_cribl_lake_dataset", "lake_id"),
					resource.TestCheckResourceAttrPair("data.criblio_cribl_lake_dataset.by_id", "id", "criblio_cribl_lake_dataset.my_cribl_lake_dataset", "id"),
				),
			},
		},
	})
}
